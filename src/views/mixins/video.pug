//- video: mixin의 이름, 괄호안의 video: mixin이 받게 될 데이터
mixin video(video)
  div
    h4
      a(href=`/videos/${video.id}`)=video.title
    ul
      li #{video.rating}/5
      li #{video.comments} comments.
      li Posted #{video.createdAt}.
      li #{video.views} views.